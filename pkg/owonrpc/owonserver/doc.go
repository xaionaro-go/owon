// Package owonserver exposes a typed instrument interface through gRPC with bounded
// handler admission, subscription polling, FIFO queues, and serialized message sizes.
//
// Example: a daemon registers NewServer's result with the generated service registrar.
package owonserver
